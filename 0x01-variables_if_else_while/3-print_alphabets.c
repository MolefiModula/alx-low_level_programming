#include <stdio.h>

/**
 * main - Prints the alphabet in lower casee
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;
	char d;

	c = 'a';
	d = 'A';
	while
		(c <= 'z')
		{
			putchar(c);
			c++;
		}
	while
		(c <= 'Z')
		{
			putchar(c);
			d++;
		}
	return (0);
}
